# Claude AI — use AWS Bedrock (recommended) or direct Anthropic API
AWS_REGION=us-west-2
ANTHROPIC_API_KEY=

# Database (SQLite path — use absolute path for production persistence)
SE_ASSIST_DATABASE_URL=sqlite:///se_assist.db

# Admin email (can delete any email/transcript)
SE_ASSIST_ADMIN_EMAIL=cchin@twilio.com
